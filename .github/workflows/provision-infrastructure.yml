name: Prepare terraform files
on:
  push:
    branches:
      - main
    paths:
      - 'SPEC.md'
  workflow_dispatch:
permissions:
  contents: write
  pull-requests: write
jobs:
  implementation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v4
        with:
          node-version: 'latest'
      - uses: hashicorp/setup-terraform@v3
      - run: mkdir ~/.codex
      - run: echo $OPENAI_TOKEN > ~/.codex/auth.json
        env:
          OPENAI_TOKEN: ${{ secrets.OPENAI_TOKEN }}
      - run: |
          CODING_SPEC=$(curl -fsSL https://raw.githubusercontent.com/pigeon-project/pigeon/refs/heads/main/agents-spec/terraform-apply-agent-spec.md)
          npx --yes @openai/codex exec --full-auto "$CODING_SPEC"
        env:
          GOOGLE_APPLICATION_CREDENTIALS: ${{ secrets.TERRAFORM_SA }}
